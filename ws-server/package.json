{
  "name": "ws-server",
  "bin": "dist/server.js",
  "tsup": {
    "entry": [
      "src/server.ts"
    ]
  },
  "scripts": {
    "dev": "tsup --watch --onSuccess 'node dist/server.js'",
    "build": "tsup"
  },
  "devDependencies": {
    "socket.io": "^4.5.4",
    "tsup": "^6.5.0"
  },
  "dependencies": {
    "zeromq": "^5"
  }
}
