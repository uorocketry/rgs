[project]
name = "gps-ingest"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.3.2",
    "odrive>=0.6.10.post0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0.0",
]
